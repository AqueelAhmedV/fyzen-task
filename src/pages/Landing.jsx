import { Typography, Container } from "@mui/material";

export default function Landing() {
    return (
        <Container>
            <Typography variant="h2">
                Landing Page
            </Typography>
        </Container>
    );
}